# WO-056 — Native Texture-Array Qualification and Production Admission

- Work-order ID: `WO-056`
- Status: Phase O Outcome A accepted; Bundle 130 live array admission accepted; Phase P fragment explicit-LOD compatibility active
- Baseline branch: `agent/ios-proof-of-life`
- Baseline repository/ledger HEAD: `68f0c1c57714f2229982add60543443a68234aae`
- Phase H candidate: `281eb237d0d9f5387814b3fdfa740524aeac459a`, Bundle 114
- Phase I candidate: `bc4b2b7181b3111053f14ff86e8ff634718acf30`, Bundle 116
- Phase I ledger commit: `8c9fd723fcaa3aff77178a8496ba82427cc28881`
- Current phase: Phase P - ordinary-runtime fragment explicit-LOD shader compatibility
- First incomplete step: build the representative BmodelSolid/StudioSolid/GrassDlight source-lineage and capability table from Diffusion assembly through GL4ES conversion to the Apple compiler before changing runtime code

## Authority and objective

Adopt the complete Phase H evidence. Determine the exact source-proven origin of `GL_INVALID_OPERATION` in the native texture-array sampling/readback stage and correct the complete failing contract in one coherent change. Preserve every device-proven PASS stage and the revised normal-Diffusion bootstrap. This is not authorization for a succession of one-call patches or a separate instrumentation-only IPA.

The goal is a build-qualified candidate whose existing harness still creates and uploads distinct array layers, translates and reflects `sampler2DArray`, draws four quadrants from layers 0, 1, 2, and 3 through the real native ES3/GL4ES route, reads back the rendered pixels, verifies the source-derived expected checksum, exercises deletion/recreation and lifecycle ownership, emits a bounded terminal result, and exits before Diffusion module or gameplay startup.

## Required audit before implementation

1. Trace every call and state transition in the sampling/readback stage from the last successful shader/reflection operation through framebuffer and attachment setup, viewport/scissor, program and uniform binding, texture-unit and `GL_TEXTURE_2D_ARRAY` binding, vertex/index/attribute state, draw submission, synchronization, read-buffer selection, pixel-pack state, `glReadPixels`, checksum calculation, state restoration, and lifecycle continuation.
2. Build a call-level error-provenance table. For every potentially failing call, record the exact wrapper/native entry point, active target and object identities, framebuffer and drawable ownership, legal GLES version and arguments, required preconditions, current state provider, and whether GL4ES forwards, translates, emulates, or caches the operation. Prove that the reported `0x0502` belongs to this stage and is not stale from an earlier successful stage.
3. Audit framebuffer completeness and format compatibility, draw/read framebuffer binding, draw-buffer/read-buffer legality, attachment dimensions and sample counts, default-versus-offscreen framebuffer semantics, viewport dimensions, shader link/use state, sampler uniform type and unit, layer-coordinate generation, vertex-array and attribute validity, pixel format/type, pack alignment, destination-buffer size, and any required finish/fence ordering on Apple GLES3.
4. Trace the checksum contract to its source data. Record the exact per-layer colors or patterns, quadrant-to-layer mapping, drawable or offscreen dimensions, coordinate origin, channel order, row order, and expected checksum derivation. The correction must not merely bless checksum `a915906d`, weaken comparison, or accept uninitialized/unchanged pixels.
5. Prove whether the defect is in the harness's GLES3 call sequence, the GL4ES wrapper's state/forwarding route, the direct-drawable integration, or an interaction among them. Audit all relevant cleanup and state restoration so the fix cannot make lifecycle PASS by leaking objects or skipping the tested operation.

## Authorized implementation

Implementation is authorized only after the audit identifies one complete structural cause. Correct that cause at its proper owner. If call-level attribution markers are needed, add the complete set to the same repaired candidate; do not publish a separate probe candidate. Each marker must identify the call, relevant object/framebuffer state, result, and immediately attributable error without hiding or draining unrelated application errors outside the self-test-owned scope.

Preserve the locked arguments exactly:

`-dev 2 -log -game diffusion -ref gl4es -gl4es_texture_array_selftest`

Preserve `gameinfo-ready`, the 57-item contract, `renderer-ready`/`dispatched` ordering, all existing upload/object/shader/lifecycle assertions, clean bounded shutdown, and unflagged ordinary startup.

## Scope and prohibited changes

Do not convert the test to CPU sampling, ordinary 2D textures, layer-zero-only sampling, an atlas, multiple unrelated textures, or a fake checksum. Do not remove `glReadPixels`, ignore or clear `GL_INVALID_OPERATION` without correcting its cause, weaken the expected result, skip framebuffer completeness, bypass GL4ES, advertise `GL_EXT_texture_array` to Diffusion, enable terrain, start gameplay, or alter `ch1map1`, controls, unrelated rendering, or game data.

Do not contact Arjun, request device evidence, retest Bundle 114, create a probe-only IPA, launch Diffusion gameplay, enable terrain, advertise arrays to Diffusion, alter `ch1map1`, begin a later phase, or create more than one qualifying candidate.

## Validation and rejection requirements

- Add a machine-readable sampling/readback contract tied to exact source calls, owners, state preconditions, expected output, and cleanup. Validate that every required call is ordered after context and shader readiness and before lifecycle teardown.
- Add positive and mutation/rejection fixtures for stale-error attribution, incomplete/wrong framebuffer, wrong attachment target or level, wrong draw/read framebuffer, illegal read-buffer state, wrong sampler unit/type, missing array binding, wrong layer coordinate, invalid vertex state, wrong viewport, unsupported pixel format/type, bad pack alignment or buffer size, missing synchronization when required, checksum weakening, skipped readback, CPU/2D/atlas fallback, state leakage, and continuation after failure.
- Retain and replay the exact pinned GL4ES and SDL patch stacks plus every renderer-contract, normal-bootstrap, drawable, object, upload, shader, lifecycle, architecture, and IPA validator. Every formerly passing Bundle 114 harness stage must remain semantically unchanged and enforced.
- Run Python compilation, all positive and rejection fixtures, `git diff --check`, affected translation-unit builds, full iPhoneOS arm64 engine/Half-Life/Diffusion qualification, IPA-contract inspection, embedded-marker verification, thin-arm64 inspection, and proprietary-game-data exclusion.
- Prove unflagged Half-Life and Diffusion startup remains unchanged. Prove the flagged route still exits before `CL_LoadProgs` or any module, map, terrain, cutscene, or gameplay admission.

## Acceptance criteria and outcome gates

### Outcome A

The exact `GL_INVALID_OPERATION` cause and checksum contract are source-proven, the complete sampling/readback correction is implemented, all retained and new rejection suites pass, and exactly one candidate is fully build-qualified. Retain exactly one qualifying workflow/artifact, publish the exact unsigned IPA once, independently verify filename/size/SHA-256, update and read back both durable ledgers, and stop. The candidate is not device-accepted; do not request testing.

### Outcome B

The exact failing call or structural cause cannot be proven from source and the existing device evidence. Make no runtime change, workflow, artifact, IPA, or upload. Record the unresolved alternatives, the minimal complete evidence needed to distinguish them, and why one repaired candidate cannot responsibly be produced. Stop for orchestrator review.

### Outcome C

The audit proves that correct native array sampling/readback requires a broader GL4ES renderer migration or cannot preserve the already passing object/upload/shader/lifecycle contracts. Do not publish a partial workaround. Record the exact architectural boundary and responsible larger alternative in both ledgers, then stop.

## Required evidence and stop conditions

Report adoption of Phase H Outcome B, the distinction between bounded exit and hard crash, selected Phase I outcome, exact source-proven error origin, checksum derivation, implementation and ledger commits, exact files changed, preserved PASS stages, validation and rejection results, workflow/artifact/IPA/tempfile metadata and SHA-256 when Outcome A applies, expected markers, remaining risks, and explicit stop state.

Append the complete result to `Documentation/XASH3DIOS_PORTING_STATE.md` and the authoritative Google Docs ledger, verify both by readback, push authorized commits to `agent/ios-proof-of-life`, and stop at the orchestrator-review gate.

## Completed Outcome A

- Exact logged origin: `gl4es_glUniform1i(u_Array, 0)`.
- Structural cause: `GL_SAMPLER_2D_ARRAY` was missing from GL4ES `uniformsize`, `is_uniform_int`, and `n_uniform`, causing incompatible uniform size/class handling and deterministic `GL_INVALID_OPERATION`.
- Additional complete-route defect: the old harness used a CPU client pointer with `glVertexAttribPointer`, invalid in native OpenGL ES 3; Bundle 116 uses a VBO-backed quad.
- Checksum contract: FNV-1a-32 over red, green, magenta, and yellow RGBA quadrant bytes equals `0xA915906D`, exactly matching Bundle 114's successful readback pixels.
- Machine-readable contract: `scripts/ios/wo56i-sampling-readback-contract.json`.
- Candidate: `bc4b2b7181b3111053f14ff86e8ff634718acf30`.
- Qualifying workflow: `32489923843`, job `96794910555`, success.
- Artifact: `Xash3DiOS-arm64-unsigned`, ID `9449473335`.
- IPA: `xash3d-fwgs-ios-arm64.ipa`, `8,716,506` bytes, SHA-256 `4FD8D67DDAEBF1986AC795164B7CD20BA782319B9F29200C9EA76F1A4BA73806`.
- Both durable ledgers were updated and read back.
- Bundle 116 is build-qualified diagnostics-only evidence and is not device-accepted.

## Exact files changed by Phase I

- `ref/gl/gl_texture_array_selftest.c`
- `scripts/ios/gl4es-wo56-texture-array-ios.patch`
- `scripts/ios/validate-ios-renderer-contract.py`
- `scripts/ios/validate-ios-selftest-boot.py`
- `scripts/ios/validate-ios-texture-array.py`
- `scripts/ios/verify_ipa.sh`
- `scripts/ios/wo56i-sampling-readback-contract.json`
- `Documentation/XASH3DIOS_PORTING_STATE.md` in the reporting commit

## Current amendments/addenda

- Raw Phase H log verification: `engine(20260821-122858).log`, SHA-256 `2A0A70CC3005795626ADF1597E656FBE30FEBEFBF0EACE0D9342BD09399FB32B`, confirms one bounded launch, 57 contract items, upload/object/shader/lifecycle PASS, checksum `a915906d`, terminal failure, and clean intentional shutdown.
- Phase I Outcome A proves that the checksum was already the exact successful expected pixel stream; the failure was API error state, not failed sampling or presentation.
- The Phase J addendum superseded the prior device-test prohibition only for the single exact Bundle 116 run. That authorization is now consumed. No terrain admission, later phase, rerun, or additional implementation is authorized.

## References

- [Current state](../Documentation/CURRENT_STATE.md)
- [DEC-001](../Decisions/DEC-001.md)
- [DEC-002](../Decisions/DEC-002.md)
- [DEC-003](../Decisions/DEC-003.md)
- [DEC-004](../Decisions/DEC-004.md)
- [DEC-005](../Decisions/DEC-005.md)
- [WO-056 evidence manifest](../Evidence/WO-056/manifest.md)

## Orchestrator acceptance and Phase J addendum

WO-056 Phase I Outcome A is accepted. The source-proven GL4ES uniform-classification defect, VBO correction, checksum derivation, validation suite, exact-pin replay, successful arm64 workflow, artifact identity, and two-ledger report are accepted as build-qualified diagnostics evidence. This acceptance does not device-accept Bundle 116 and does not admit Diffusion terrain or gameplay.

### Phase J objective

Qualify or reject the already-built Bundle 116 native texture-array sampling/readback contract on one physical iOS device through the preserved normal Diffusion game-information bootstrap. Reuse the existing IPA exactly; do not create a new candidate, workflow, artifact, upload, patch, instrumentation change, or source edit.

### Exact candidate

- Candidate commit: `bc4b2b7181b3111053f14ff86e8ff634718acf30`
- Workflow: `32489923843`, success
- Artifact: `Xash3DiOS-arm64-unsigned`, ID `9449473335`
- IPA: `xash3d-fwgs-ios-arm64.ipa`, `8,716,506` bytes
- IPA SHA-256: `4FD8D67DDAEBF1986AC795164B7CD20BA782319B9F29200C9EA76F1A4BA73806`
- Locked arguments: `-dev 2 -log -game diffusion -ref gl4es -gl4es_texture_array_selftest`

### Authorized device procedure

1. Verify the IPA filename, byte count, and SHA-256 before installation. A mismatch is Outcome C; do not run it.
2. Preserve the device's existing authorized external Diffusion data. Do not repackage, replace, add, or delete game data for this test.
3. Install Bundle 116 over the existing test installation without deleting its data.
4. Launch the diagnostic route exactly once. Do not click Start, enter gameplay, select a map, or interact with menus. The harness is expected to terminate the app intentionally after its bounded result.
5. Export the complete new engine log from that one launch. Record the device model, iOS version, and whether the process visibly closed after the terminal marker.
6. If the process terminates without a complete terminal marker, also return the matching iOS `.ips` report if one exists. Do not infer a hard crash merely from the app closing.
7. Stop after the evidence handoff. Do not repeat the launch unless a later orchestrator instruction explicitly authorizes it.

### Outcome A — device acceptance

Accept Bundle 116 only if the one complete log proves the normal Diffusion bootstrap, all 57 contract items, one renderer dispatch, every retained policy/object/upload/shader/lifecycle PASS stage, `sampling-fbo ... result=PASS`, every immediate `sampling-call` with `error=0x0000 result=PASS`, the exact sampling contract, checksum `a915906d`, terminal `PASS failures=0 diffusion_started=0`, and bounded intentional shutdown. This qualifies the native texture-array conformance harness only. Terrain remains disabled and unadvertised; gameplay and `ch1map1` remain outside the result.

### Outcome B — device rejection

Reject Bundle 116 if a complete run reaches the harness but records any nonzero immediate error, framebuffer failure, checksum other than `a915906d`, missing retained PASS stage, terminal FAIL, nonzero failure count, nonzero `diffusion_started`, module/map/terrain/gameplay admission, or a matching iOS crash report. Preserve the exact first failing call and all later bounded evidence. Do not patch, rebuild, or rerun.

### Outcome C — inconclusive evidence

Stop as inconclusive if the IPA identity is wrong, external Diffusion game information is unavailable, installation or log export fails, the log is truncated before the decisive boundary without a matching `.ips`, or the evidence cannot be tied to one Bundle 116 launch. Do not reinterpret this as renderer failure and do not rerun without review.

### Required evidence

- Verified IPA filename, size, and SHA-256
- Device model and iOS version
- Complete engine log from the sole authorized launch, with its filename, byte count, and SHA-256
- Exact first divergent marker if Outcome B applies
- Matching `.ips` only if unexpected process termination occurred and one exists
- Explicit Outcome A, B, or C conclusion and stop state

### Prohibited changes and stop conditions

No source, patch, validator, CI, workflow, artifact, IPA, game-data, renderer, terrain, menu, input, gameplay, or `ch1map1` change is authorized. Do not test Bundle 114, do not launch ordinary gameplay, and do not begin another phase. The worker may request the one evidence package above, validate it, update the durable records, and report; it must otherwise remain stopped.

## Phase J Outcome A device report

- Device: iPhone 16 Pro Max running iOS 26.6; log-reported GPU `Apple A18 Pro`; drawable `2868x1320`.
- Sole authorized launch: began `20:55:04` and stopped `20:55:05` with candidate marker `bc4b2b71-dirty` and the exact locked arguments.
- Evidence: `1-engine.log`, `23,255` bytes, `254` lines, SHA-256 `139B15982FEC0B4D34146B3F99A39D4758B36D77925394403B214BE9F5544FF5`.
- Bootstrap: real Diffusion game information loaded; all 57 contract items completed; renderer became ready and dispatched once; no `CL_LoadProgs`, module, map, terrain, cutscene, or gameplay admission occurred.
- Retained stages: mutable, immutable, compressed upload, object identity, shader translation/reflection, and lifecycle all reported PASS.
- Sampling: `sampling-fbo` reported complete direct-drawable state; immediate calls `seq=1` through `seq=51` all reported `error=0x0000 result=PASS`, including the corrected `glUniform1i(u_Array)` and VBO-backed `glVertexAttribPointer` routes.
- Pixel proof: four quadrants from layers `0,1,2,3` retained exact checksum `a915906d` and reported PASS.
- Terminal: `iOS texture array selftest terminal: PASS failures=0 diffusion_started=0`.
- Shutdown: `Note: Issuing host shutdown due to reason "iOS texture array selftest complete"`, followed by `CL_Shutdown()`, context-destroy lifecycle cleanup, and `Stopped with reason "iOS texture array selftest complete"`. The user-observed one-second close was the intentional bounded exit, not a hard crash; no `.ips` is required.
- The later `GL_EXT_texture_array - failed` capability line remains expected because production Diffusion advertisement is deliberately disabled. It does not contradict the native self-test PASS.

### Phase J conclusion and stop state

**Outcome A. Bundle 116 is device-accepted for the native texture-array conformance contract on iPhone 16 Pro Max / iOS 26.6.** This qualification covers the bounded normal-bootstrap harness only. Production Diffusion terrain remains disabled and unadvertised; ordinary gameplay and the quarantined `ch1map1` transition remain unqualified by this result.

The sole device-test authorization is consumed. Do not rerun, rebuild, patch, enable or advertise terrain, launch a production gameplay experiment, change `ch1map1`, or begin a later phase without a new explicit orchestrator order. Stop at the orchestrator-review gate.

## Orchestrator acceptance and Phase K order

WO-056 Phase J Outcome A is accepted. Bundle 116 is device-qualified for the bounded native texture-array conformance contract. The observed one-second close was the harness's intentional shutdown. The later `GL_EXT_texture_array - failed` line is the first unqualified production boundary, not a contradiction of the conformance result.

### Phase K — Conditional production texture-array admission

- **Status:** Active; one bounded audit, coherent implementation, validation, and build-qualification candidate are authorized.
- **Baseline:** branch `agent/ios-proof-of-life` at Phase J Outcome A record `cc02ebaa192abb3a11ce5ea520649a096a68ed44`; qualified implementation baseline `bc4b2b7181b3111053f14ff86e8ff634718acf30`.
- **Objective:** admit the device-qualified native Apple GLES3/GL4ES texture-array capability into the real engine and Diffusion production route only when the complete source-proven capability contract is satisfied. Preserve the real landscape array loader and terrain shader/material feature set. Produce at most one build-qualified candidate and stop before any device test.

### Required audit and provenance contract

Before or with implementation, produce a source-proven end-to-end table covering:

1. The native ES3 context/version and every array entry point required by object creation, mutable/immutable/compressed upload, subimage, deletion, sampler binding, draw, and limit queries; identify whether GL4ES forwards, translates, caches, or emulates each operation.
2. The exact GL4ES extension-string/capability provider and the conditions under which `GL_EXT_texture_array` may be exposed. Prove unsupported contexts/backends and missing-entry-point cases remain unadvertised.
3. The engine `GL_CheckExtension` route, `GL_TEXTURE_ARRAY_EXT`, exported callbacks, `GL_MAX_ARRAY_TEXTURE_LAYERS_EXT`, `GL_SetTextureTarget`, `GL_CreateTextureArray`, and `GL_LoadTextureArray` invariants.
4. Diffusion's independent `R_TEXTURE_ARRAY_EXT` gate, `GLSL_ALLOW_TEXTURE_ARRAY`, `LOAD_TEXTURE_ARRAY`, `LoadTerrainLayers`, diffuse/normal landscape arrays, maximum layer use, texture targets, sampler uniforms, and cleanup/ownership.
5. The production terrain shader path using `sampler2DArray`, `texture2DArray`, `TERRAIN_NUM_LAYERS`, and `BMODEL_MULTI_LAYERS`, including the current iOS shader-directive filtering. Prove Phase K does not advertise arrays while stripping or disabling the shader feature that consumes them.
6. Engine/Diffusion agreement and failure behavior: one side must not claim support when the other side lacks the complete contract, and ordinary Half-Life/Diffusion startup must remain unchanged when the contract is false.

Record this as `scripts/ios/wo56k-production-array-admission-contract.json` or a semantically equivalent machine-readable contract whose exact path is reported.

### Authorized implementation scope

After the audit proves one complete bounded route, implement the minimum coherent production admission at the proper owners. Conditional GL4ES capability exposure, engine and Diffusion gates, required limit/procedure validation, and removal of an iOS-only terrain-shader suppression are in scope only where the provenance contract proves they are necessary parts of the same admission.

Retain the exact pinned GL4ES and SDL patch stacks. Preserve all established renderer contracts, including Bundle 69 direct-drawable ownership, Bundle 71 native unsigned-index handling, Bundle 81 per-unit texture-target state, Bundle 96 material/inactive-sampler behavior, and Bundle 116 array uniform/VBO/sampling behavior. Preserve the diagnostic harness and its exact Phase J PASS semantics.

### Prohibited changes

- Do not unconditionally append or spoof `GL_EXT_texture_array`, claim support from an unsupported context/backend, or bypass a missing function/limit.
- Do not add a cvar, command-line override, environment override, or hard-coded Diffusion-side force-enable.
- Do not replace production arrays with CPU sampling, 2-D textures, atlases, multiple unrelated textures, or a layer-zero-only route.
- Do not make shaders compile by removing terrain, `MULTI_LAYERS`, normal-layer, or other required landscape semantics. Do not silently reduce the production feature contract.
- Do not change gameplay, menus, input, game data, maps, cutscenes, unrelated shader families, or `ch1map1`.
- Do not request or perform a device test, ordinary gameplay launch, or terrain runtime qualification in Phase K.
- Do not create a probe-only IPA, a succession of partial advertisement candidates, or more than one qualifying candidate/workflow/artifact.

### Validation and rejection requirements

- Add positive and mutation/rejection coverage for conditional extension exposure; unsupported context/backend; missing required proc; invalid/zero/insufficient maximum layers; engine/Diffusion capability disagreement; absent array callbacks; wrong texture target; lost `GLSL_ALLOW_TEXTURE_ARRAY`; stripped `BMODEL_MULTI_LAYERS`; bypassed `LoadTerrainLayers`; missing diffuse or normal array path; sampler/type/unit mismatch; false-positive support; 2-D/atlas/CPU fallback; and changes to ordinary non-array startup.
- Retain and run the full texture-array contract and rejection suite, normal-bootstrap and 57-item renderer-contract suites, direct-drawable, unsigned-index, index-trace, WO49 topology/transform, per-unit target, WO52 material, lifecycle, architecture, and IPA validators.
- Replay the exact pinned patch stacks from clean pins. Run Python compilation/JSON parsing, `git diff --check`, affected translation-unit builds, and full iPhoneOS arm64 engine/Half-Life/Diffusion qualification.
- Inspect the packaged IPA for the new production-admission contract/markers where applicable, thin arm64 architecture, required dylibs, and proprietary-game-data exclusion. Prove unflagged Half-Life and Diffusion builds remain available.

### Acceptance criteria and outcome gates

#### Outcome A — coherent production admission build-qualified

The audit establishes one complete conditional capability route; the implementation admits texture arrays only when all required provider, entry-point, limit, engine, Diffusion, loader, and shader conditions hold; every retained and new rejection suite passes; and exactly one candidate completes full arm64/IPA qualification. Retain exactly one qualifying workflow/artifact, publish the exact unsigned IPA once if the established project workflow requires it, verify filename/size/SHA-256, update and read back both durable ledgers, and stop at orchestrator review. The result is build-qualified only and is not terrain/device-accepted.

#### Outcome B — broader boundary discovered

The audit proves that coherent production admission requires a broader renderer/shader/material migration than this bounded phase. Do not partially advertise the extension and do not publish a runtime candidate, workflow, artifact, IPA, or upload. Record the exact boundary, responsible owners, already-qualified prerequisites, and the smallest next decision required in both ledgers; stop for orchestrator review.

#### Outcome C — capability cannot be safely discriminated

The available source cannot safely distinguish the supported Apple GLES3/GL4ES route from unsupported providers or cannot establish engine/Diffusion agreement. Make no runtime change or build. Record the unresolved alternatives and exact evidence needed; stop for orchestrator review.

### Required report and stop conditions

Report the selected outcome; baseline; complete provenance table; exact capability predicate; shader/material admission finding; implementation and ledger commits; exact files changed; validation and rejection results; pinned-stack replay; workflow/run/job/artifact; IPA filename, bytes, SHA-256, and mirror metadata when Outcome A applies; retained regression contracts; known risks; and explicit stop state.

Append the outcome to `Documentation/XASH3DIOS_PORTING_STATE.md` and the authoritative Google Docs ledger, verify both by readback, push authorized commits to `agent/ios-proof-of-life`, and stop at the orchestrator-review gate. Do not ask Arjun for device evidence and do not begin a later phase.

## Phase K worker-reported Outcome A

- Status: build-qualified; awaiting orchestrator acceptance. Bundle 124 is not terrain/device-accepted.
- Coherent implementation: `c063202dc0c0111304e2d0a82a2506f1457f1454`.
- Qualification corrections: `12a80912c8c18870cad71e6116fbfeda2e26e2c3`, `38d429b189efc9a46e1a47f8463bf04797641fd6`, and candidate `976c38f3d99d7ef6eaf348188fabf4fe4e722be9`.
- Worker-report commit: `a067869505778dcabd8ab47b6b2e583892f8ce36`.
- Contract: `scripts/ios/wo56k-production-array-admission-contract.json`.
- Qualifying workflow: `32510363562`, job `96859751554`, success on exact candidate `976c38f3d99d7ef6eaf348188fabf4fe4e722be9`.
- Retained artifact: `Xash3DiOS-arm64-unsigned`, ID `9456949434`, archive SHA-256 `5ed24f8a6ad27dfaea62ed315329d8bdf79e95c5a0053a4ba36187236f42b744`.
- IPA: `xash3d-fwgs-ios-arm64.ipa`, Bundle 124, `8,717,677` bytes, SHA-256 `FB62C2903E21152DCB74C709656F8FEF841ACE2A22C281F27B90AC02F0E1D04F`.
- Negative CI evidence: runs `32508365615`, `32509025360`, and `32509723819` failed at bounded pre-artifact qualification boundaries and are preserved; no artifact from those runs is retained.
- Verified route: conditional live ES3+/proc/layer-limit/ESSL-300 GL4ES provider; independent engine proc/limit gate; independent Diffusion callback/limit gate; complete weight/diffuse/normal landscape arrays and cleanup; retained real multi-layer terrain shader/material features.
- Validation: exact pins replayed; positive and rejection suites passed; full iPhoneOS arm64 engine/Half-Life/Diffusion build and IPA owner/architecture/proprietary-data checks passed.
- Remaining boundary: no physical-device production landscape load, shader execution, rendering, transition, or gameplay evidence exists.

### Phase K stop state

The worker is stopped at the orchestrator-review gate. Do not launch or retest Bundle 124, request Arjun evidence, run a device/gameplay test, modify terrain or `ch1map1`, start another workflow/candidate/upload, or begin another phase without a new explicit orchestrator-authored order.

## Orchestrator acceptance and Phase L order

WO-056 Phase K Outcome A is accepted as build-qualified evidence. Candidate `976c38f3d99d7ef6eaf348188fabf4fe4e722be9`, workflow `32510363562`, artifact `9456949434`, and Bundle 124 establish one coherent conditional production-admission implementation and package. They do not establish live device admission, terrain loading, terrain shader execution, rendering, transition behavior, or gameplay.

### Phase L — Bundle 124 normal-bootstrap production texture-array admission device acceptance

- **Status:** Active; evidence-only. Exactly one ordinary Diffusion launch of exact Bundle 124 is authorized on the established iPhone 16 Pro Max / iOS 26.6.
- **Candidate:** `976c38f3d99d7ef6eaf348188fabf4fe4e722be9`.
- **Workflow/artifact:** `32510363562` / `Xash3DiOS-arm64-unsigned` ID `9456949434`.
- **IPA:** `xash3d-fwgs-ios-arm64.ipa`, Bundle 124, `8,717,677` bytes, SHA-256 `FB62C2903E21152DCB74C709656F8FEF841ACE2A22C281F27B90AC02F0E1D04F`.
- **Locked arguments:** `-dev 2 -log -game diffusion -ref gl4es`. The self-test flag must be absent.
- **Objective:** determine whether the exact build-qualified provider, engine, and Diffusion production texture-array admission gates enable and agree during an ordinary physical-device startup, before any terrain, map, transition, or gameplay execution.

### Authorized evidence procedure

1. Verify the IPA filename, byte count, and SHA-256 before installation. A mismatch is Outcome C; do not launch it.
2. Preserve the existing authorized external Diffusion data. Install Bundle 124 over the existing app without deleting or changing its data.
3. Confirm the locked arguments above and the absence of `-gl4es_texture_array_selftest` or any other diagnostic/force-enable flag.
4. Launch exactly once and allow the ordinary Diffusion bootstrap to reach a stable menu. Do not click Start, New Game, a difficulty, or any other route that can admit a map or gameplay.
5. Wait approximately 10 seconds at the stable menu, capture one screenshot, then close/export without starting gameplay.
6. Export the complete engine log from that sole launch. Record its filename, byte count, SHA-256, device model, iOS version, and whether the app remained stable at the menu.
7. Supply a matching iOS `.ips` only if the process terminates unexpectedly and one exists. Do not rerun without orchestrator review.

### Required admission evidence

The complete log must contain one coherent enabled set:

- `iOS production texture array provider:` with native ES major at least 3, required procedures present, `max_layers` at least 16, ESSL 300 available, `advertised=1`, and `reason=complete`.
- `iOS production texture array engine:` with all four required procedures, `max_layers` at least 16, `minimum=16`, and `enabled=1`.
- `iOS production texture array admission:` with `extension=1`, `callbacks=1`, `max_layers` at least 16, `minimum=16`, `terrain_shaders=full`, and `enabled=1`.

The three markers must agree. The run must not contain `GL_EXT_texture_array - failed`, `Landscapes will be unavailable`, a relevant GL/shader failure, or an unexpected crash. The menu must remain visibly stable for the bounded observation.

### Outcome gates

#### Outcome A — live production admission accepted

The exact IPA identity matches, the sole complete run reaches a stable menu, all three markers are present and agree on the complete enabled contract, the prohibited warning/failure lines are absent, and there is no relevant GL/shader failure or unexpected termination. This qualifies only physical-device production texture-array admission during ordinary bootstrap. It does not qualify the landscape loader, terrain objects, terrain shaders in use, drawing, map transition, gameplay, or `ch1map1`.

#### Outcome B — live production admission rejected

The exact candidate reaches renderer initialization but any required marker is false, mismatched, or missing after initialization; a prohibited capability/landscape warning appears; a relevant GL/shader failure occurs; or the app crashes. Preserve the first divergent marker and all later evidence. Do not patch, rebuild, relaunch, or proceed into terrain/gameplay.

#### Outcome C — inconclusive evidence

The IPA identity, arguments, data state, installation, screenshot, or log export is wrong or unverifiable; the log is truncated; or the evidence cannot be tied to exactly one launch. Do not treat this as renderer rejection and do not rerun without orchestrator review.

### Scope, prohibited changes, and stopping conditions

This phase is evidence-only. Do not modify source, patches, validators, workflows, CI, artifacts, IPA contents, game data, menus, input, controller behavior, terrain, maps, transitions, gameplay, or `ch1map1`. Do not launch a build or CI workflow, upload another artifact, create a new candidate, or use a self-test/force-enable route. Do not click Start or otherwise enter gameplay.

The preserved worker may request exactly the evidence package above, validate it, select Outcome A/B/C, update and read-back verify ControlPlane plus both durable ledgers, commit and push documentation-only reporting, and stop at orchestrator review. After confirming local/remote equality and the stop gate, the worker must message the delegating orchestrator directly with the selected outcome, final commit, evidence identities, qualification boundary, first incomplete step, and explicit stop state.

### Required evidence package

- Verified IPA filename, byte count, and SHA-256
- Device model and iOS version
- One stable-menu screenshot from the sole authorized run
- Complete engine log with filename, byte count, SHA-256, and the three production-admission markers
- First divergent marker if Outcome B applies
- Matching `.ips` only for unexpected termination when one exists
- Explicit Outcome A, B, or C conclusion and stop state

### References

- [DEC-007](../Decisions/DEC-007.md)
- [WO-056 evidence manifest](../Evidence/WO-056/manifest.md)

### Phase L worker report — Outcome C

- **Outcome:** C, pre-launch argument identity failure. The exact candidate cannot execute the Phase L ordinary-bootstrap procedure as written.
- **IPA identity:** locally retained `xash3d-fwgs-ios-arm64.ipa`, Bundle 124, `8,717,677` bytes, SHA-256 `FB62C2903E21152DCB74C709656F8FEF841ACE2A22C281F27B90AC02F0E1D04F`; these values match the authorized tuple.
- **Source proof:** `engine/platform/ios/launchdialog.m` sets the launch field to `-dev 2 -log -game diffusion -ref gl4es -gl4es_texture_array_selftest`, immediately calls `setEnabled:NO`, and derives `argv` from that field. The user cannot remove the self-test flag in Bundle 124.
- **Package proof:** the exact extracted Bundle 124 `xash` executable contains both the full self-test argument string and `-gl4es_texture_array_selftest`; `Info.plist` reports bundle version `124`.
- **Required-versus-actual invariant:** Phase L requires `-dev 2 -log -game diffusion -ref gl4es` with the self-test flag absent. The exact IPA enforces the prohibited flag, so step 3 cannot pass and launching would violate the order rather than test production admission.
- **Device evidence:** none. The app was not installed or launched, no screenshot/log/`.ips` was requested or produced, and the sole launch authorization remains unconsumed. This is not renderer rejection and does not change the accepted Phase K build result.
- **Qualification boundary:** Bundle 116 remains device-qualified for bounded native-array conformance; Bundle 124 remains build-qualified for conditional production admission. Ordinary physical-device production-admission agreement remains unqualified.
- **First incomplete step:** orchestrator review of the contradictory candidate/argument tuple and a decision whether any separately authorized ordinary-argument build is justified. No patch, build, CI, artifact, upload, device launch, gameplay action, or later phase is authorized here.
- **Stop state:** Phase L is complete at Outcome C and the orchestrator-review gate. Do not launch or retest Bundle 124, change its arguments/data, patch, build, start CI, create an artifact, enter terrain/gameplay, or begin another phase.

## Orchestrator acceptance and Phase M order

WO-056 Phase L Outcome C is accepted as a correct pre-launch guard result. The immutable Bundle 124 tuple is internally contradictory for Phase L: its IPA identity is correct, but its sole disabled launch field enforces `-gl4es_texture_array_selftest`. The one-run device authorization remains unconsumed. This finding does not reject the Phase K production-admission implementation.

### Phase M — Locked ordinary-bootstrap candidate build qualification

- **Status:** Active; one bounded audit, minimal implementation, validation, and build-qualified candidate are authorized. No device test is authorized.
- **Control baseline:** branch `agent/ios-proof-of-life` at Phase L Outcome C commit `c7be0a237ad223e60b7808a903d2411a9154c153`.
- **Qualified production baseline:** candidate `976c38f3d99d7ef6eaf348188fabf4fe4e722be9`, workflow `32510363562`, artifact `9456949434`, Bundle 124 IPA SHA-256 `FB62C2903E21152DCB74C709656F8FEF841ACE2A22C281F27B90AC02F0E1D04F`.
- **Objective:** produce one package-identifiable iOS candidate that preserves the complete Phase K production texture-array route but boots with the exact locked ordinary arguments `-dev 2 -log -game diffusion -ref gl4es`, with no automatic self-test flag, then stop before runtime/device qualification.

### Required audit and machine-readable contract

Before or with implementation, prove and record:

1. The complete iOS argument route from `IOS_LaunchDialog` through `IOS_GetArgs` into `Host_Main`, including why the disabled launch field is authoritative for the candidate.
2. Every source, validator, mutation fixture, build step, and IPA string check that currently encodes the self-test launch tuple or its required dormancy.
3. The exact distinction between the default launch tuple and retained diagnostic machinery: the default must omit `-gl4es_texture_array_selftest`, while the flag parser, harness, terminal path, markers, and explicit conditional guards remain compiled and inert unless the flag is explicitly supplied by some separately authorized future route.
4. The package-level discriminator: the executable must contain one exact standalone ordinary launch string and must not contain the combined ordinary-plus-self-test launch string. Mere substring presence is insufficient because the ordinary tuple is a prefix of the old diagnostic tuple.
5. The unchanged Phase K provider/engine/Diffusion markers and capability predicates.

Record these invariants in `scripts/ios/wo56m-ordinary-bootstrap-contract.json` or a semantically equivalent machine-readable contract whose exact path is reported.

### Authorized implementation scope

- In `engine/platform/ios/launchdialog.m`, replace the locked diagnostic launch value with exactly `-dev 2 -log -game diffusion -ref gl4es`. Keep the field disabled so this candidate has one immutable, auditable argument tuple. A constant rename that accurately reflects ordinary use is allowed.
- Update only the directly affected validators, rejection fixtures, contract files, and IPA verifier so they require the exact standalone ordinary tuple, reject the combined self-test tuple as the default, and prove the diagnostic harness remains compiled but dormant.
- Add one focused Phase M validator if that produces a clearer separation than weakening a historical self-test validator. Retain the complete historical self-test conformance checks as regression evidence where semantically applicable.
- Build and package the normal engine, Half-Life, and Diffusion arm64 outputs through the existing workflow. Retain exactly one qualifying workflow/artifact and verify the resulting unsigned IPA identity, size, SHA-256, bundle version, executable strings, owners, architecture, and proprietary-data exclusion.

### Prohibited changes

- Do not change GL4ES, renderer capability predicates, engine/Diffusion admission gates, array loader behavior, terrain shaders, materials, texture units, samplers, or cleanup.
- Do not delete, bypass, auto-run, or reinterpret the self-test harness, its explicit flag gate, terminal path, or accepted Bundle 116 evidence.
- Do not restore editable launch settings, alter `settings.bin` behavior, add an argument selector, cvar, environment switch, alternate dialog action, fallback, or hidden force-enable path. This phase changes only the locked tuple needed for the bounded candidate.
- Do not change game data, menus beyond the immutable launch text value, touch/input/controller behavior, maps, cutscenes, terrain, transitions, gameplay, or `ch1map1`.
- Do not request or perform a device launch, screenshot, log capture, terrain test, gameplay test, or reuse the Phase L authorization. It remains unconsumed and does not transfer automatically to a new candidate.
- Do not modify workflows merely to force a build, create a probe-only IPA, publish serial partial candidates, or retain more than one qualifying artifact.

### Validation and rejection requirements

- Add positive and mutation coverage for exact ordinary tuple equality; absence of the combined self-test tuple; locked-field authority; `IOS_LaunchDialog` → `IOS_GetArgs` → `Host_Main` flow; dormant self-test flag parsing; accidental unconditional self-test dispatch; removal of the harness/terminal/markers; Valve or other game substitution; editable-field restoration; argument override/fallback; changed production capability predicates; and package substring false positives.
- `verify_ipa.sh` must use an exact-line or semantically equivalent package discriminator for the standalone ordinary tuple, explicitly reject the old combined tuple, and separately prove the self-test flag/gates and three production-admission markers remain in their correct packaged owners.
- Retain and run Python compilation, JSON parsing, all positive and rejection suites, `git diff --check`, exact-pin patch replay, affected translation-unit checks, the 57-item renderer contract, texture-array object/upload/shader/sampling/lifecycle suites, normal-bootstrap, direct-drawable, uint-index, per-unit-target, material/inactive-sampler, shader, architecture, IPA, and proprietary-data gates.
- Perform full iPhoneOS arm64 engine, Half-Life, and Diffusion client/server/menu qualification. No device runtime evidence is part of Phase M.

### Acceptance criteria and outcome gates

#### Outcome A — ordinary-argument candidate build-qualified

The audit proves one bounded argument route; the implementation changes only the locked launch tuple and directly affected validation/contract surfaces; the standalone ordinary string is exact in source and package; the combined diagnostic launch string is absent; diagnostic machinery remains compiled and flag-gated; all retained and new rejection suites pass; and exactly one candidate completes full arm64/IPA qualification. Retain one qualifying workflow/artifact, publish the unsigned IPA once through the established route, verify filename/size/SHA-256 and mirror round-trip if used, update and read back ControlPlane plus both ledgers, and stop at orchestrator review. The candidate is build-qualified only and is not device-, terrain-, gameplay-, or `ch1map1`-accepted.

#### Outcome B — broader launch-path coupling discovered

The audit proves that producing a truthful ordinary-argument candidate requires broader launch-dialog/settings, harness, renderer, data, or workflow changes than the bounded tuple/validation scope. Do not make a partial runtime change or start CI. Record the exact coupling, responsible owners, smallest next decision, and why the candidate was not produced; update/read back durable records and stop.

#### Outcome C — package dormancy cannot be proven

The source/package surfaces cannot reliably distinguish the standalone ordinary tuple from the old combined diagnostic tuple or cannot prove the self-test remains retained but dormant. Make no runtime change or build. Record the missing discriminator and exact evidence needed; update/read back durable records and stop.

### Required report and stop conditions

Report the selected outcome; baseline; argument ownership table; machine-readable contract path; implementation and reporting commits; exact files changed; positive/rejection results; retained regression suites; exact pins and replay result; workflow/run/job/artifact; IPA filename, bundle version, bytes, SHA-256, mirror metadata where applicable; packaged ordinary/self-test/production marker findings; absence of device evidence; qualification boundary; first incomplete step; and explicit stop state.

Update and read-back verify `Documentation/CURRENT_STATE.md`, this work-order record, relevant Decisions/Evidence records, `Documentation/XASH3DIOS_PORTING_STATE.md`, and the authoritative Google Docs ledger. Commit and push authorized changes to `agent/ios-proof-of-life`, confirm clean local/remote equality, message the delegating orchestrator with the mandatory completion callback, and stop at the orchestrator-review gate. Do not request device evidence or begin a later phase.

### References

- [DEC-008](../Decisions/DEC-008.md)
- [WO-056 evidence manifest](../Evidence/WO-056/manifest.md)

## Phase M completed Outcome A

- Implementation commit: `5a529ff41d23b557e6e4e7878fb31284c7dfc661`.
- Argument ownership is source-proven: `IOS_LaunchDialog` assigns the exact tuple to the sole disabled field and tokenizes it into `szArgc/szArgv`; `IOS_GetArgs` exports those values; `main` passes them unchanged to `Host_Main`.
- Machine contract: `scripts/ios/wo56m-ordinary-bootstrap-contract.json`.
- Exact source tuple: `-dev 2 -log -game diffusion -ref gl4es`; the combined default ending in `-gl4es_texture_array_selftest` is absent. The flag parser, conditional host/renderer dispatch, harness, terminal, and markers remain compiled and dormant without the explicit flag.
- Sole retained qualifying workflow: [32551387441](https://github.com/arjunyerevan95-dot/Xash3DiOS/actions/runs/32551387441), job [96978610370](https://github.com/arjunyerevan95-dot/Xash3DiOS/actions/runs/32551387441/job/96978610370), success. Automatic PR duplicate `32551389885` was cancelled during dependency installation before build or artifact creation.
- Retained artifact: [Xash3DiOS-arm64-unsigned](https://github.com/arjunyerevan95-dot/Xash3DiOS/actions/runs/32551387441/artifacts/9470194619), ID `9470194619`, archive size `8,617,082` bytes, digest `sha256:400cde7cf33dc29b6ca72e0376f6d05bc979202622e1e3b98d5cb3d7126e3eef`.
- Exact unsigned IPA: `xash3d-fwgs-ios-arm64.ipa`, Bundle 126, `8,717,507` bytes, SHA-256 `F5FE061690C0532C3086B2CCD650E541FCEB14F35128F7A35C66B670AE105ACF`.
- Tempfile mirror: [information page](https://tempfile.org/MrnVs2sedq5/), [direct IPA](https://tempfile.org/MrnVs2sedq5/download), ID `MrnVs2sedq5`, expiry epoch-ms `1787545730208`; metadata/security and direct-download readback reproduce the filename, size, SHA-256, risk `safe`, and no warning/suspicious pattern.
- Independent package inspection found one exact ordinary string in `xash`, zero combined strings, one separate self-test flag token, all three production markers in their proper owners, 13 thin-arm64 Mach-O objects, and no proprietary game assets.
- No device launch, screenshot, log, terrain, map, transition, gameplay, or `ch1map1` action occurred. Bundle 126 is build-qualified only.

Stop state: **Work Order 56 Phase M Outcome A is complete at the orchestrator-review gate.** Do not request device evidence, launch Bundle 126, reuse the Phase L authorization, build another candidate, or begin a later phase without a new explicit orchestrator-authored order.

## Phase M acceptance and Phase N device result

The orchestrator accepts Phase M Outcome A as exact ordinary-argument build qualification. After that review, the orchestrator explicitly authorized one bounded Bundle 126 physical-device run in chat: install the exact IPA on the established iPhone 16 Pro Max / iOS 26.6, launch normally, select Start once, wait up to 30 seconds, do not relaunch after a crash, and return the resulting screenshot/log plus a matching `.ips` if one exists. This authorization did not alter source or build evidence. The completed run is materialized here as **WO-056 Phase N** so it is not left only in chat.

### Phase N exact identity and procedure

- Candidate: `5a529ff41d23b557e6e4e7878fb31284c7dfc661`; final Phase M reporting baseline: `308978fde8f7904c3511ccd6eed6bfb5c975ad5c`.
- Workflow/job/artifact: `32551387441` / `96978610370` / `9470194619`; GitHub reports the workflow successful and the artifact unexpired.
- IPA: `xash3d-fwgs-ios-arm64.ipa`, Bundle 126, `8,717,507` bytes, SHA-256 `F5FE061690C0532C3086B2CCD650E541FCEB14F35128F7A35C66B670AE105ACF`.
- Device: iPhone 16 Pro Max, iOS 26.6, Apple A18 Pro GPU; drawable `2868x1320`.
- Runtime log identity: `5a529ff4-dirty`, `agent/ios-proof-of-life`, `apple-arm64`; exact arguments `(null) -dev 2 -log -game diffusion -ref gl4es`.
- Exactly one run was performed. The user observed that Diffusion launched and loaded, rendered a severely incomplete scene for a few seconds, and then terminated. No rerun is authorized or needed.

### Phase N observed boundary and outcome

Positive evidence is preserved. Diffusion data mounted; engine, server, renderer, UI and client modules initialized; `maps/ch1map0.bsp` loaded; the first three bounded map-trace frames returned through client view, renderer, audio, debug and frame-complete; world uint-index ingress matched its native realization; and the screenshot proves drawable presentation of sky, water and intro text. This advances beyond the old immediate-start failure and is not a blank-screen or launcher failure.

The first deterministic production-admission divergence occurs before terrain use:

- The packaged `iOS production texture array provider:` marker is absent from `engine.log`. Absence alone does not prove the provider function did not execute because execution and log transport have not yet been separated.
- The engine reports `GL_EXT_texture_array - failed` and `iOS production texture array engine: procedures=4 max_layers=0 minimum=16 enabled=0`.
- Diffusion reports `iOS production texture array admission: extension=0 callbacks=1 max_layers=0 minimum=16 terrain_shaders=full enabled=0`, followed by `GL_EXT_texture_array not supported. Landscapes will be unavailable`.
- No `GL_INVALID_OPERATION` is recorded, so the already-corrected Phase I sampler-uniform defect is not observed in this run.

Later evidence is real but is downstream of the selected Phase O boundary. `GL_EXT_shader_texture_lod` is rejected by the live shader compiler; `texture2DLodEXT` is undeclared in Bmodel/Studio fragment variants; and 593 `StudioSolid` draws are rejected because `u_StudioParams` metadata/program linkage is unavailable. This explains much of the missing scene. The log reaches at least frame 56 and ends after further vegetation-surface creation without an engine fatal, assertion, signal, or orderly shutdown marker. The exact termination mechanism remains unresolved.

Evidence identities:

- Screenshot `1-Photo-1.jpg`: `53,760` bytes; SHA-256 `9FF030CE0FA2471AF3A7DE354C610A1E1C9DC41CE02878B3DDE88EBE0AABBB47`.
- Complete log `2-engine.log`: `189,696` bytes; 2,976 lines; SHA-256 `F1888BC343ADECEFF74A697B7C9E8B73A6D3F0271E336D5D4BD9B929F479459D`.
- Supplied crash report `xash-2026-08-21-120428.ips`: `28,954` bytes; SHA-256 `CE2FF0E4938E929B6C2A5308BFE5F48FAE745120CC9637F14ADF85510D9310F9`; timestamp August 21 and Bundle 105. It is explicitly rejected as evidence for the August 22 Bundle 126 termination.

Selected result: **Phase N Outcome B.** Bundle 126 fails physical-device production texture-array admission and visible-scene qualification. Bundle 116 remains device-qualified for the bounded native-array harness; Bundle 126 remains build/argument-qualified and supplies partial ordinary map/presentation evidence. Terrain correctness, complete object rendering, stability, transitions, gameplay and `ch1map1` remain unqualified.

## Orchestrator acceptance and Phase O order

The orchestrator accepts Phase N Outcome B and selects only the earliest deterministic unresolved boundary: **WORK ORDER 56 PHASE O - ORDINARY-RUNTIME GL4ES TEXTURE-ARRAY PROVIDER ADMISSION.** Phase O is one bounded source/provenance audit plus, only if one complete structural cause is proven, the minimum coherent correction and one build-qualified candidate. It is not a shader-LOD, Studio model, foliage, crash, terrain-visual, transition or gameplay order.

### Baseline, objective, and first incomplete action

- Control baseline: `308978fde8f7904c3511ccd6eed6bfb5c975ad5c`; qualified implementation candidate: `5a529ff41d23b557e6e4e7878fb31284c7dfc661`.
- Accepted prerequisite: Bundle 116 / Phase J exact native array conformance on the same device.
- Failed live boundary: Bundle 126 ordinary startup sees four engine callbacks but no extension token and a zero layer limit, so engine and Diffusion admission remain disabled.
- Objective: establish why the live qualified provider state is not available to ordinary GL4ES extension construction and engine limit queries, correct that cause at its responsible owner if source-proven, and stop with at most one build-qualified ordinary candidate.
- First incomplete action: create a call/lifecycle provenance table covering context creation/currentness, `initialize_gl4es`, `GetHardwareExtensions`, native version/procedure/limit discovery, ESSL 300 discovery, `hardext.texture_array` and `hardext.maxarraylayers` writes/resets, `gl4es_texture_array_available()`, `BuildExtensionsList` construction/caching, wrapper `glGetString`/`glGetIntegerv`, engine `GL_CheckExtension`, and Diffusion admission. Compare the Phase J diagnostic call route against the Phase N ordinary route.

Record the resulting invariant in `scripts/ios/wo56o-provider-lifecycle-contract.json` or a semantically equivalent machine-readable contract whose exact path is reported.

### Required audit

1. Prove the exact SDL/UIKit/EAGL context ownership and current-context state at every GL4ES provider-discovery and extension-list construction point. Determine whether provider discovery runs too early, is never rerun after context currentness, or is overwritten later.
2. Trace every write and read of `native_es_major`, `hardext.texture_array`, `hardext.maxarraylayers`, `hardext.glsl300es`, and the extension-list cache. Record ordering, lifetime, invalidation and rebuild behavior across initialization, restoration and teardown.
3. Prove how `gles_getProcAddress`, `gles_glGetIntegerv(GL_MAX_ARRAY_TEXTURE_LAYERS)`, wrapper `glGetIntegerv(GL_MAX_ARRAY_TEXTURE_LAYERS)`, and `gl4es_texture_array_available()` differ or agree. Do not infer live capability from packaged strings.
4. Explain why Bundle 116's explicit diagnostic route obtained complete native-array behavior while Bundle 126's ordinary route reports zero production layers. Identify any direct-native diagnostic bypass, initialization side effect, cache timing, or provider/engine ownership difference.
5. Distinguish provider-marker execution from transport. `SHUT_LOGD` absence in `engine.log` is not sufficient proof that `GetHardwareExtensions` did not execute. Audit the existing GL4ES-to-engine console callback lifetime and choose a durable bounded marker route only if needed for the coherent candidate.
6. Preserve the later `texture2DLodEXT` failures, 593 Studio rejections, screenshot and abrupt log end as separate evidence. Do not promote any of them to the provider root cause without a source-proven dependency.

### Authorized implementation scope

Only after the audit proves one structural cause, correct it at the smallest responsible GL4ES/engine lifecycle owner. Directly affected accepted patch-stack files, engine integration, validators, mutation fixtures, the Phase O machine contract, build replay and IPA verifier may change. A bounded engine-console provider marker is allowed only as part of the same coherent repaired candidate and must report actual live version/procedure/limit/ESSL/route/advertisement state without draining or changing GL errors.

The conditional all-required predicate remains mandatory: live native ES3+, required 3-D procedures, at least 16 live array layers, working ESSL 300, compiled GL4ES array routes, engine procedure/limit agreement, and Diffusion token/callback/limit agreement. False predicates must remain false and stable.

### Prohibited changes

- No unconditional `GL_EXT_texture_array`, fabricated or hard-coded layer limit, force-enable cvar/argument/environment path, or provider result copied from the device-qualified self-test without a live ordinary-context query.
- No direct native-array bypass in engine or Diffusion; no ordinary 2-D, atlas, CPU, layer-zero, disabled-landscape or other fallback.
- Do not alter `-dev 2 -log -game diffusion -ref gl4es`, launcher settings, the diagnostic flag/harness/checksum/terminal route, or Bundle 116 accepted evidence.
- Do not change Diffusion `GL_EXT_shader_texture_lod` handling, `texture2DLodEXT`, Bmodel/Studio shaders, `u_StudioParams`, material filtering, model drawing, vegetation, crash handling or logging beyond the permitted provider marker.
- Do not change game data, menus, input/controllers, maps, cutscenes, transitions, gameplay or `ch1map1`.
- Do not request or perform a device test, contact the user for evidence, launch a second exploratory workflow before the audit gate, retain multiple qualifying candidates, or begin a later phase.

### Validation and rejection requirements

- Add positive and mutation coverage for current ES3 context ordering; provider discovery before/after context currentness; missing/replaced procedures; zero/insufficient/live layer limits; ESSL 300 failure; unavailable array route; stale or early extension-list cache; provider state reset; wrapper/native limit disagreement; marker transport without state mutation; engine/Diffusion disagreement; and every prohibited unconditional/force/bypass/fallback route.
- Prove the ordinary tuple does not arm the self-test and the diagnostic tuple remains separately valid. Retain the 57-item renderer contract, direct drawable, uint index, per-unit target, material-state, inactive sampler, shader, lifecycle and all Phase I/K/M texture-array/admission/package gates.
- Replay the exact GL4ES, Diffusion, SDL, MainUI and executable pins. Run Python compilation/JSON parsing, the complete positive/rejection suite, `git diff --check`, affected translation-unit builds, full iPhoneOS arm64 engine/Half-Life/Diffusion qualification, IPA contract/marker-owner inspection, thin-arm64 inspection and proprietary-data exclusion.
- If Outcome A is selected, retain exactly one qualifying push workflow/artifact; cancel or delete automatic duplicate artifacts; publish the unsigned IPA once; independently verify its filename, bundle number, bytes and SHA-256 plus one tempfile round trip; update/read-back verify ControlPlane and both ledgers; then stop.

### Outcomes

- **Outcome A:** one complete source-proven lifecycle/provider cause is corrected at its owner; all retained/new rejection gates pass; exactly one ordinary candidate is build-qualified. This does not device-accept provider admission, terrain, rendering or stability.
- **Outcome B:** the audit proves the necessary correction requires a broader context-lifecycle/provider interface or invalidation redesign that cannot fit this bounded phase without weakening accepted ownership. Make no partial runtime change/build; record the exact boundary and smallest responsible later alternative.
- **Outcome C:** source and existing evidence cannot distinguish early discovery, state reset/cache, route availability, native/wrapper query or marker transport. Make no runtime change/build. Record the minimal complete discriminator; do not publish an instrumentation-only IPA unless a later order explicitly authorizes it.

### Required report and stop conditions

Report the selected outcome; complete provenance table; accepted/rejected hypotheses; exact files/commits; validation and rejection results; workflow/job/artifact/IPA/tempfile identity when applicable; preserved Phase N evidence; remaining shader/termination boundaries; first incomplete next action; and explicit stop state.

Update and read-back verify `Documentation/CURRENT_STATE.md`, this work-order record, `DEC-009`, `Evidence/WO-056/manifest.md`, `Documentation/XASH3DIOS_PORTING_STATE.md`, and the authoritative Google Docs ledger. Commit and push authorized changes to `agent/ios-proof-of-life`, confirm a clean worktree and local/remote equality, send the mandatory completion callback to the delegating orchestrator, and stop at orchestrator review.

Stop state: **Work Order 56 Phase O is active.** No Phase O device test or later phase is authorized.
## Work Order 56 Phase O worker report — Outcome A

Selected outcome: **Outcome A — coherent production admission build-qualified.** Starting ControlPlane/remote head was `4591b6753ca068185c7edb2be62348a5be99692e`. The structural implementation is `15b831ae6a25d79a01cff0a2d14c53e13cd9f89a`; the exact candidate head, including the rejection-fixture correction, is `f42f2c96b61624fe510fe32288bfbfa6873cc686`. Candidate Bundle 130 is build-qualified only, not device-, terrain-, scene-, or stability-accepted.

### Verified boundary, provenance, and structural cause

The first deterministic Phase N boundary is ordinary GL4ES provider admission before engine/Diffusion array consumption. Source tracing proves this lifecycle:

| Stage | Owner and call | Proven result |
| --- | --- | --- |
| Context create/current | SDL/UIKit/EAGL `SDL_GL_CreateContext` -> `SDL_GL_MakeCurrent` | Requested ES3 context is current before renderer extension initialization. |
| Renderer entry | `VID_SetMode` -> `GL_OnContextCreated` | GL4ES get-proc callback is registered before provider initialization. |
| Provider initialization | `initialize_gl4es` -> `GetHardwareExtensions` | With `NO_INIT_CONSTRUCTOR`, discovery runs once against the live current context. |
| Native capability | `glGetString(GL_VERSION)`, three 3-D proc lookups, `GL_MAX_ARRAY_TEXTURE_LAYERS` | Native ES major, required procs, and live layer limit are cached conditionally. |
| ESSL capability | old `testGLSL("#version 300 es", 0)` | The generic helper concatenated `#version 300 es` directly with malformed `#extension require GL_IMG_uniform_buffer_object...` text, so the ESSL 300 probe could not establish `hardext.glsl300es`. |
| Extension construction | `BuildExtensionsList` | Its correct all-required predicate short-circuited on false `glsl300es`, omitting `GL_EXT_texture_array`. |
| Engine admission | wrapper `glGetString` -> `GL_CheckExtension` -> layer query | The missing token prevented the engine limit query; Bundle 126 therefore reported four callbacks but `max_layers=0 enabled=0`. |
| Diffusion admission | engine extension/callback/limit export | Diffusion correctly rejected landscapes from the disabled engine contract. |
| Teardown | `close_gl4es` -> route reset -> `ResetHardwareExtensions` | Provider and extension cache are destroyed before a future context generation. |

Bundle 116’s diagnostic self-test and Bundle 126’s ordinary route share the same current context and provider discovery. The diagnostic harness directly exercised the wrapper array operations and did not depend on production extension-token admission; the ordinary engine/Diffusion path did. The absent old provider marker was a transport issue: `SHUT_LOGD` is suppressed by the shared-library `nobanner=1` policy and is not an engine.log sink.

The complete predicate remains: live ES3+, `glTexImage3D`, `glTexSubImage3D`, and `glTexStorage3D`; at least 16 live array layers; successful valid ESSL 300 compile; compiled GL4ES array route; engine token/callback/limit agreement; and Diffusion token/callback/limit/terrain-shader agreement.

### Correction and why it addresses the cause

The GL4ES provider now compiles one complete, stage-correct ESSL 300 vertex source with a newline after `#version 300 es`. It does not force the result. The provider caches native-major/procedure/route provenance and exports a read-only snapshot that performs no GL call, error drain, extension rebuild, or cache mutation. The engine prints that snapshot through its durable console transport before its existing independent admission gate. The extension token, limit, wrapper route, engine gate, Diffusion gate, full texture-array consumer, locked ordinary arguments, and dormant diagnostic harness remain conditional and unchanged.

Machine contract: `scripts/ios/wo56o-provider-lifecycle-contract.json`. Expected runtime markers:

- `iOS production texture array provider: native_es_major=<>=3 procedures=1 max_layers=<>=16 minimum=16 glsl300=1 route=1 advertised=1 source=live-context`
- `iOS production texture array engine: procedures=4 max_layers=<>=16 minimum=16 enabled=1`
- `iOS production texture array admission: extension=1 callbacks=1 max_layers=<>=16 minimum=16 terrain_shaders=full enabled=1`

No Phase O runtime/device result is claimed.

### Exact files and commits

Implementation commit `15b831ae6a25d79a01cff0a2d14c53e13cd9f89a`:

- `ref/gl/gl_opengl.c`
- `scripts/gha/build_ios.sh`
- `scripts/ios/gl4es-wo56-provider-lifecycle-ios.patch`
- `scripts/ios/validate-ios-provider-lifecycle.py`
- `scripts/ios/validate-ios-renderer-contract.py`
- `scripts/ios/validate-ios-selftest-boot.py`
- `scripts/ios/wo56m-ordinary-bootstrap-contract.json`
- `scripts/ios/wo56o-provider-lifecycle-contract.json`

Validation-only candidate commit `f42f2c96b61624fe510fe32288bfbfa6873cc686`:

- `scripts/ios/validate-ios-production-array-admission.py`

Reporting changes are limited to `Documentation/CURRENT_STATE.md`, `Documentation/XASH3DIOS_PORTING_STATE.md`, `WorkOrders/WO-056.md`, `Decisions/DEC-009.md`, and `Evidence/WO-056/manifest.md`; the final reporting commit is the commit containing this report and is identified in the Google ledger, callback, and final worker handoff.

### Validation and CI

- Exact GL4ES pin `81547d986798e876de8b434193920b606a72363f` replayed cleanly through all twelve retained patches and the new Phase O patch, then reversed to a clean nested checkout.
- Eleven locally runnable retained/Phase O validators passed with mutation suites: uint elements, index trace, WO49 topology/transform/per-unit texture target, WO52 material trace, texture array, provider lifecycle, self-test boot, ordinary bootstrap, and the 57-item renderer contract.
- The Phase K production-admission suite was replayed against pinned, fully patched Diffusion `14d156bf3a6993c172697fac83a937836c3b5561` and rejected the tightened zero-layer fixture. Python compilation, JSON parsing, and `git diff --check` passed.
- The SDL drawable validator could not run in the local top-level checkout because its CI-fetched SDL source was absent; both successful CI validation and full build covered it.
- Push run `32569704879` and PR duplicate `32569706595` failed before artifact creation because the pre-existing Phase K zero-layer fixture loosely matched a second legitimate `>=16` expression introduced by the read-only snapshot. The exact provider-assignment fixture was corrected without a runtime change.
- Sole qualifying workflow: [32570119378](https://github.com/arjunyerevan95-dot/Xash3DiOS/actions/runs/32570119378), success on exact head `f42f2c96b61624fe510fe32288bfbfa6873cc686`; job [97024299913](https://github.com/arjunyerevan95-dot/Xash3DiOS/actions/runs/32570119378/job/97024299913). Automatic PR iOS duplicate `32570121887` was cancelled and produced no artifact; deployment workflows skipped.
- CI passed every source/rejection/pin gate, full iPhoneOS arm64 engine, Half-Life, Diffusion client/server/menu build, IPA contract, marker-owner, architecture, and proprietary-data checks.

### Artifact and IPA

- Retained GitHub artifact: [Xash3DiOS-arm64-unsigned](https://github.com/arjunyerevan95-dot/Xash3DiOS/actions/runs/32570119378/artifacts/9475150885), ID `9475150885`, archive size `8,620,189` bytes, digest `sha256:0ff55313fc563a301563af10724de5ced9555d518a24768f46c2673e47099293`, expiry `2026-09-05T11:27:53Z`.
- IPA: `xash3d-fwgs-ios-arm64.ipa`, Bundle 130, `8,718,358` bytes, SHA-256 `9FD6E3DD7E8FE19B4B3987479D2E69FFD99EF7FF4368FD1F9884286BB095BB5D`.
- Exactly one tempfile.org object: [information page](https://tempfile.org/EA5v8CtY9bT/) and [direct IPA download](https://tempfile.org/EA5v8CtY9bT/download), ID `EA5v8CtY9bT`, expiry `2026-08-24T11:37:45.305Z`. Metadata/security readback matches filename, size, and SHA-256, reports `safe`, no warning, and no suspicious patterns. A fresh direct download reproduced the exact bytes and hash.
- Independent extraction confirmed Bundle 130; main executable, GL4ES renderer, and Diffusion client/server/menu are thin arm64 Mach-O; all three production markers are present in the correct owner binaries; no proprietary game data is packaged.

### Remaining risks and stop state

Bundle 130 is build-qualified only. The valid ESSL 300 provider probe, enabled provider/engine/Diffusion agreement, landscape availability, terrain creation/shader execution, scene output, later shader-LOD/Bmodel/Studio failures, process termination, transitions, gameplay, and `ch1map1` have no Phase O physical-device qualification. Bundle 126 was not retested.

Single device test/evidence request: **none authorized or requested in Phase O**. The orchestrator may decide in a later explicit order whether a bounded ordinary-bootstrap admission observation is justified.

First incomplete step: orchestrator review of Outcome A and an explicit next decision. Stop state: **Work Order 56 Phase O is complete at the orchestrator-review gate.** Do not contact Arjun, request evidence/testing, launch Bundle 130, run another workflow, create another artifact/upload, address later shader/scene/termination evidence, or begin another phase.


Authoritative Google ledger Phase N/Phase O issuance append: revision-guarded and verified by readback at revision `AIroW34aaNvQXMEgSa7x-l1eTqapjucXUj7nWl7rYAVamGDQKOk4YpYSSIBbEDA5_eZcuAhIlBdmkfCdh9xViN5R1a3p26kB_PqnzcSQWsM`.

## Orchestrator acceptance, Bundle 130 device evidence, and Phase P order

The orchestrator accepts Phase O Outcome A as coherent build qualification. Exact candidate `f42f2c96b61624fe510fe32288bfbfa6873cc686`, reporting head `78ae00a86b2938943b8c0d2f7ba6846bee6b7538`, workflow `32570119378`, job `97024299913`, artifact `9475150885`, and Bundle 130 IPA identity remain accepted. The Phase O ESSL 300 provider correction is not reopened.

A subsequent user-performed Bundle 130 ordinary run is accepted as unplanned but valid evidence; it does not retroactively alter Phase O's no-device-test authority. The established iPhone 16 Pro Max / iOS 26.6 reports runtime `f42f2c96-dirty`, branch `agent/ios-proof-of-life`, `apple-arm64`, and exact arguments `(null) -dev 2 -log -game diffusion -ref gl4es`.

### Accepted live array-admission boundary

- Provider: `native_es_major=3 procedures=1 max_layers=2048 minimum=16 glsl300=1 route=1 advertised=1 source=live-context`.
- Engine: `GL_EXT_texture_array` enabled; `procedures=4 max_layers=2048 minimum=16 enabled=1`.
- Diffusion: `extension=1 callbacks=1 max_layers=2048 minimum=16 terrain_shaders=full enabled=1`.
- Live ordinary provider/engine/Diffusion texture-array admission is qualified for Bundle 130. Actual landscape creation, terrain shader execution, complete scene rendering, and stability are not.

### First remaining divergence and termination boundary

The native extension string logged by GL4ES contains `GL_EXT_shader_texture_lod`, but 44 fragment compilations reject the extension, 396 `texture2DLodEXT` calls are undeclared, Bmodel/Studio/Grass program families fail, and 593 `StudioSolid` submissions are rejected because the relevant programs are unlinked. The screenshot remains an incomplete sky/water/cloud/text scene.

The user directly observed a hard crash. The engine log contains no fatal, signal, assertion, orderly shutdown, or crash marker and ends after frame 56 with the render gate open. No matching Bundle 130 `.ips` is supplied. The hard-crash mechanism remains downstream and unresolved; Phase P must not attribute or repair it without a source-proven relationship and matching evidence.

Evidence:

- Screenshot: `55,609` bytes; SHA-256 `C88414F8C5B66644D645E13F6B60A3B1DF94FCD0084F4A7793E58B57CE4D7ED9`.
- Complete `engine.log`: `196,032` bytes; 3,055 lines; SHA-256 `A4D92F07FCC2401C615B7179D45A06EB01289007D40AFDF537B3324510ACAE47`.
- No rerun is requested.

## Work Order 56 Phase P — ordinary-runtime fragment explicit-LOD shader compatibility

### Baseline and objective

- Branch / ControlPlane baseline: `agent/ios-proof-of-life` at `78ae00a86b2938943b8c0d2f7ba6846bee6b7538`.
- Exact build-qualified and device-observed candidate: `f42f2c96b61624fe510fe32288bfbfa6873cc686`, Bundle 130, workflow `32570119378`, artifact `9475150885`.
- Accepted prerequisite: Phase O provider/engine/Diffusion array admission.

Prove the exact source and version semantics from Diffusion's assembled desktop GLSL through GL4ES `glShaderSource` / `ConvertShader` to the native Apple ESSL compiler. Explain why `hardext.shaderlod` selects an EXT directive/intrinsic path that the live compiler rejects. Only if one complete structural cause is proven, correct the explicit-LOD compatibility contract at its responsible translator or shader-contract owner.

### First incomplete action

Produce a source-lineage and capability table for representative failing `BmodelSolid`, `StudioSolid`, and `GrassDlight` fragment variants. Record:

1. Diffusion source, includes, defines, and assembled `#version 130` source.
2. Original GL4ES input and the selected `ConvertShader` target/source version.
3. Relevant provider state: native ES/ESSL version, `hardext.shaderlod`, `hardext.cubelod`, settings, and extension-string provenance.
4. Emitted native `#version`, extension directives, explicit-LOD intrinsic spelling, and source fingerprint.
5. Native compile result, program/link state, cache ownership, and downstream consumer behavior.
6. The shared `texfetch.h` lineage and every affected Bmodel/Studio/Grass owner.

Do not modify runtime code before this table identifies the complete shared boundary.

### Required audit

1. Trace `GL_ProcessShader`, `GL_LoadGPUShader`, `pglShaderSourceARB`, GL4ES `gl4es_glShaderSource`, `ConvertShader`, native `gles_glShaderSource` / `gles_glCompileShader`, and result propagation into `GL_CreateUberShader` and program caches.
2. Distinguish desktop GLSL 130 `texture2DLod`, ESSL 100 plus `GL_EXT_shader_texture_lod` / `texture2DLodEXT`, and ESSL 300 core `textureLod` semantics.
3. Prove why the extension appears in the native extension string while the emitted shader directive is rejected. Audit context/compiler source version, `hardext.shaderlod` discovery, `noshaderlod`, conversion branches, and core-promotion semantics.
4. Determine whether the failure is one shared GL4ES conversion defect, a Diffusion source-contract defect, a mixed-version path, or multiple independent defects. Do not patch families independently before shared ownership is known.
5. Preserve source/compile provenance without changing GL errors, compile/link outcomes, feature admission, or rendering. If bounded fingerprints or path markers are necessary, include them in the same coherent repaired candidate rather than an instrumentation-only IPA.
6. Keep the hard crash as separate unresolved evidence.

### Authorized implementation

Only after one complete structural cause is proven, apply the smallest coherent correction at the translator or shader-contract owner. Preserve explicit mip-LOD semantics and every accepted texture-array/provider gate. Add `scripts/ios/wo56p-shader-lod-compatibility-contract.json` or the semantic equivalent, complete positive/mutation/rejection/pin/build/package coverage, and retain at most one qualifying ordinary candidate/workflow/artifact/IPA.

### Prohibited changes

- No implicit texture sampling, constant/layer-zero result, CPU sampling, 2-D/atlas fallback, or disabled shader families.
- No deleting the directive while leaving incompatible EXT intrinsics, blind global token renaming, forced/fabricated compiler capability, error suppression, or accepting uncompiled/unlinked programs.
- No per-family Bmodel/Studio/Grass workaround when the shared converter/include contract owns the defect.
- Do not alter accepted array admission or its markers, ordinary arguments, diagnostic harness, materials/model data, vegetation policy, maps, menus, input, transitions, gameplay, `ch1map1`, crash handling, or platform lifecycle.
- Do not request or perform a Phase P device test, contact the user for evidence, begin the crash investigation, or start a later phase.

### Acceptance criteria and validation

- Create the Phase P machine contract and representative/shared-family lineage.
- Add positive and mutation/rejection fixtures for source version, extension/core branch, directive placement, intrinsic spelling, `hardext.shaderlod` / `cubelod`, missing or false extension, ES2/ES3 differences, no-shader-lod mode, converter bypass, source-order corruption, semantic weakening, partial-family patching, unlinked-program acceptance, and error suppression.
- Retain Phase I/J native-array conformance; Phase K/O production admission; Phase M ordinary arguments; 57-item renderer contract; direct drawable; uint-index/index trace; WO49 topology/transform/per-unit target; WO51/52 material; inactive sampler; lifecycle; architecture; marker ownership; thin-arm64; and proprietary-data exclusion.
- Replay exact GL4ES, Diffusion, SDL, MainUI, and executable pins. Run Python/JSON validation, complete positive/rejection suites, affected translation-unit builds, full iPhoneOS arm64 engine/Half-Life/Diffusion qualification, IPA contract, and `git diff --check`.

### Outcomes

- **Outcome A:** one complete source-proven explicit-LOD defect is corrected at its owner; representative/shared-family contracts pass; exactly one candidate is build-qualified and published once. It remains not device-, scene-, stability-, transition-, gameplay-, or `ch1map1`-accepted.
- **Outcome B:** the audit proves a broader shader-language migration or translator redesign is required. Make no partial runtime candidate/build/upload; record the exact boundary and minimum coherent future scope.
- **Outcome C:** existing source/evidence cannot distinguish the alternatives. Make no runtime candidate/build/upload; record the minimum complete discriminator. Do not publish an instrumentation-only IPA without a later order.

### Required evidence, callback, and stop

Report the selected outcome, source-proven cause or unresolved alternatives, source-lineage table, files/commits, validation/rejection results, workflow/job/artifact/IPA/tempfile identity if Outcome A, preserved gates, remaining risks, qualification boundary, and first incomplete step.

Update and read-back verify ControlPlane, `Documentation/XASH3DIOS_PORTING_STATE.md`, and the authoritative Google ledger; push; confirm clean local/remote equality; then message orchestrator thread `01a02450-2442-7bd3-9232-46419e80d731` on host `slingshot:env_e_6a6f826a8f4483218b6956e12dea53cc`. The callback must include outcome, final reporting commit, CI/artifact/IPA identity where applicable, qualification boundary, first incomplete step, and explicit stop state.

Stop state: **Work Order 56 Phase P is active.** No device test, crash investigation, later phase, or other engineering work is authorized.

Related records: [DEC-010](../Decisions/DEC-010.md) and [Evidence/WO-056/manifest.md](../Evidence/WO-056/manifest.md).

Authoritative Google ledger Phase O acceptance / Bundle 130 observation / Phase P order: revision-guarded and verified by readback at revision `AIroW36mLoq7WrJrz_hQ7VNAZ8_Cp4ALusa3DW2ZE0kSZO_GS5x9v3CU-c9dwzB8xNVgb24EwecG-HOmPXZeJG3_1ndUT06S-r9l7deSS2o`.

### Phase P worker-transition addendum

- Active implementation ownership transfers to the existing `Continue Work Order 56` thread `01a022ae-5ea9-7121-8512-2fe40f5e99a2` on host `slingshot:env_e_6a6f826a8f4483218b6956e12dea53cc`.
- The prior `Xash3DiOS Worker Bootstrap` thread `019ff1ea-8387-7291-b391-f030d22db2ef` is retained for historical implementation context but is no longer the active executor because it repeatedly failed at the conversation transport/policy boundary and is currently `systemError`.
- No Phase P CI run, candidate, top-level source commit, or device test was consumed before the transfer.
- Preserve and inspect the prior worker's ignored replay checkout `build/wo56m-gl4es-replay4`. Its exploratory `shaderconv.c` / `hardext.c` edits are not accepted code. They capture a potentially important cross-stage constraint: promoting only the fragment shader to ESSL 300 would pair it with an ESSL 100 vertex shader and fail linking, so the existing GL4ES program compatibility/reconversion owner must be included in the mandated source-lineage table.
- The replacement worker must adopt this repo-backed order and current-state/evidence records, reconcile its older ledger-based WO56 context against them, avoid repeating already-qualified phases, and continue only from Phase P's first incomplete step.
