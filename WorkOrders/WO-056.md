# WO-056 Phase I — Complete the Native Array Sampling/Readback Correction

- Work-order ID: `WO-056`, Phase I
- Status: Phase I Outcome A accepted; Phase J active, device evidence only
- Baseline branch: `agent/ios-proof-of-life`
- Baseline repository/ledger HEAD: `68f0c1c57714f2229982add60543443a68234aae`
- Phase H candidate: `281eb237d0d9f5387814b3fdfa740524aeac459a`, Bundle 114
- Phase I candidate: `bc4b2b7181b3111053f14ff86e8ff634718acf30`, Bundle 116
- Phase I ledger commit: `8c9fd723fcaa3aff77178a8496ba82427cc28881`
- Current phase: Phase J — Bundle 116 normal-bootstrap device acceptance
- First incomplete step: verify, install, and launch the exact existing Bundle 116 IPA once on a physical device, then return the complete engine log

## Authority and objective

Adopt the complete Phase H evidence. Determine the exact source-proven origin of `GL_INVALID_OPERATION` in the native texture-array sampling/readback stage and correct the complete failing contract in one coherent change. Preserve every device-proven PASS stage and the revised normal-Diffusion bootstrap. This is not authorization for a succession of one-call patches or a separate instrumentation-only IPA.

The goal is a build-qualified candidate whose existing harness still creates and uploads distinct array layers, translates and reflects `sampler2DArray`, draws four quadrants from layers 0, 1, 2, and 3 through the real native ES3/GL4ES route, reads back the rendered pixels, verifies the source-derived expected checksum, exercises deletion/recreation and lifecycle ownership, emits a bounded terminal result, and exits before Diffusion module or gameplay startup.

## Required audit before implementation

1. Trace every call and state transition in the sampling/readback stage from the last successful shader/reflection operation through framebuffer and attachment setup, viewport/scissor, program and uniform binding, texture-unit and `GL_TEXTURE_2D_ARRAY` binding, vertex/index/attribute state, draw submission, synchronization, read-buffer selection, pixel-pack state, `glReadPixels`, checksum calculation, state restoration, and lifecycle continuation.
2. Build a call-level error-provenance table. For every potentially failing call, record the exact wrapper/native entry point, active target and object identities, framebuffer and drawable ownership, legal GLES version and arguments, required preconditions, current state provider, and whether GL4ES forwards, translates, emulates, or caches the operation. Prove that the reported `0x0502` belongs to this stage and is not stale from an earlier successful stage.
3. Audit framebuffer completeness and format compatibility, draw/read framebuffer binding, draw-buffer/read-buffer legality, attachment dimensions and sample counts, default-versus-offscreen framebuffer semantics, viewport dimensions, shader link/use state, sampler uniform type and unit, layer-coordinate generation, vertex-array and attribute validity, pixel format/type, pack alignment, destination-buffer size, and any required finish/fence ordering on Apple GLES3.
4. Trace the checksum contract to its source data. Record the exact per-layer colors or patterns, quadrant-to-layer mapping, drawable or offscreen dimensions, coordinate origin, channel order, row order, and expected checksum derivation. The correction must not merely bless checksum `a915906d`, weaken comparison, or accept uninitialized/unchanged pixels.
5. Prove whether the defect is in the harness's GLES3 call sequence, the GL4ES wrapper's state/forwarding route, the direct-drawable integration, or an interaction among them. Audit all relevant cleanup and state restoration so the fix cannot make lifecycle PASS by leaking objects or skipping the tested operation.

## Authorized implementation

Implementation is authorized only after the audit identifies one complete structural cause. Correct that cause at its proper owner. If call-level attribution markers are needed, add the complete set to the same repaired candidate; do not publish a separate probe candidate. Each marker must identify the call, relevant object/framebuffer state, result, and immediately attributable error without hiding or draining unrelated application errors outside the self-test-owned scope.

Preserve the locked arguments exactly:

`-dev 2 -log -game diffusion -ref gl4es -gl4es_texture_array_selftest`

Preserve `gameinfo-ready`, the 57-item contract, `renderer-ready`/`dispatched` ordering, all existing upload/object/shader/lifecycle assertions, clean bounded shutdown, and unflagged ordinary startup.

## Scope and prohibited changes

Do not convert the test to CPU sampling, ordinary 2D textures, layer-zero-only sampling, an atlas, multiple unrelated textures, or a fake checksum. Do not remove `glReadPixels`, ignore or clear `GL_INVALID_OPERATION` without correcting its cause, weaken the expected result, skip framebuffer completeness, bypass GL4ES, advertise `GL_EXT_texture_array` to Diffusion, enable terrain, start gameplay, or alter `ch1map1`, controls, unrelated rendering, or game data.

Do not contact Arjun, request device evidence, retest Bundle 114, create a probe-only IPA, launch Diffusion gameplay, enable terrain, advertise arrays to Diffusion, alter `ch1map1`, begin a later phase, or create more than one qualifying candidate.

## Validation and rejection requirements

- Add a machine-readable sampling/readback contract tied to exact source calls, owners, state preconditions, expected output, and cleanup. Validate that every required call is ordered after context and shader readiness and before lifecycle teardown.
- Add positive and mutation/rejection fixtures for stale-error attribution, incomplete/wrong framebuffer, wrong attachment target or level, wrong draw/read framebuffer, illegal read-buffer state, wrong sampler unit/type, missing array binding, wrong layer coordinate, invalid vertex state, wrong viewport, unsupported pixel format/type, bad pack alignment or buffer size, missing synchronization when required, checksum weakening, skipped readback, CPU/2D/atlas fallback, state leakage, and continuation after failure.
- Retain and replay the exact pinned GL4ES and SDL patch stacks plus every renderer-contract, normal-bootstrap, drawable, object, upload, shader, lifecycle, architecture, and IPA validator. Every formerly passing Bundle 114 harness stage must remain semantically unchanged and enforced.
- Run Python compilation, all positive and rejection fixtures, `git diff --check`, affected translation-unit builds, full iPhoneOS arm64 engine/Half-Life/Diffusion qualification, IPA-contract inspection, embedded-marker verification, thin-arm64 inspection, and proprietary-game-data exclusion.
- Prove unflagged Half-Life and Diffusion startup remains unchanged. Prove the flagged route still exits before `CL_LoadProgs` or any module, map, terrain, cutscene, or gameplay admission.

## Acceptance criteria and outcome gates

### Outcome A

The exact `GL_INVALID_OPERATION` cause and checksum contract are source-proven, the complete sampling/readback correction is implemented, all retained and new rejection suites pass, and exactly one candidate is fully build-qualified. Retain exactly one qualifying workflow/artifact, publish the exact unsigned IPA once, independently verify filename/size/SHA-256, update and read back both durable ledgers, and stop. The candidate is not device-accepted; do not request testing.

### Outcome B

The exact failing call or structural cause cannot be proven from source and the existing device evidence. Make no runtime change, workflow, artifact, IPA, or upload. Record the unresolved alternatives, the minimal complete evidence needed to distinguish them, and why one repaired candidate cannot responsibly be produced. Stop for orchestrator review.

### Outcome C

The audit proves that correct native array sampling/readback requires a broader GL4ES renderer migration or cannot preserve the already passing object/upload/shader/lifecycle contracts. Do not publish a partial workaround. Record the exact architectural boundary and responsible larger alternative in both ledgers, then stop.

## Required evidence and stop conditions

Report adoption of Phase H Outcome B, the distinction between bounded exit and hard crash, selected Phase I outcome, exact source-proven error origin, checksum derivation, implementation and ledger commits, exact files changed, preserved PASS stages, validation and rejection results, workflow/artifact/IPA/tempfile metadata and SHA-256 when Outcome A applies, expected markers, remaining risks, and explicit stop state.

Append the complete result to `Documentation/XASH3DIOS_PORTING_STATE.md` and the authoritative Google Docs ledger, verify both by readback, push authorized commits to `agent/ios-proof-of-life`, and stop at the orchestrator-review gate.

## Completed Outcome A

- Exact logged origin: `gl4es_glUniform1i(u_Array, 0)`.
- Structural cause: `GL_SAMPLER_2D_ARRAY` was missing from GL4ES `uniformsize`, `is_uniform_int`, and `n_uniform`, causing incompatible uniform size/class handling and deterministic `GL_INVALID_OPERATION`.
- Additional complete-route defect: the old harness used a CPU client pointer with `glVertexAttribPointer`, invalid in native OpenGL ES 3; Bundle 116 uses a VBO-backed quad.
- Checksum contract: FNV-1a-32 over red, green, magenta, and yellow RGBA quadrant bytes equals `0xA915906D`, exactly matching Bundle 114's successful readback pixels.
- Machine-readable contract: `scripts/ios/wo56i-sampling-readback-contract.json`.
- Candidate: `bc4b2b7181b3111053f14ff86e8ff634718acf30`.
- Qualifying workflow: `32489923843`, job `96794910555`, success.
- Artifact: `Xash3DiOS-arm64-unsigned`, ID `9449473335`.
- IPA: `xash3d-fwgs-ios-arm64.ipa`, `8,716,506` bytes, SHA-256 `4FD8D67DDAEBF1986AC795164B7CD20BA782319B9F29200C9EA76F1A4BA73806`.
- Both durable ledgers were updated and read back.
- Bundle 116 is build-qualified diagnostics-only evidence and is not device-accepted.

## Exact files changed by Phase I

- `ref/gl/gl_texture_array_selftest.c`
- `scripts/ios/gl4es-wo56-texture-array-ios.patch`
- `scripts/ios/validate-ios-renderer-contract.py`
- `scripts/ios/validate-ios-selftest-boot.py`
- `scripts/ios/validate-ios-texture-array.py`
- `scripts/ios/verify_ipa.sh`
- `scripts/ios/wo56i-sampling-readback-contract.json`
- `Documentation/XASH3DIOS_PORTING_STATE.md` in the reporting commit

## Current amendments/addenda

- Raw Phase H log verification: `engine(20260821-122858).log`, SHA-256 `2A0A70CC3005795626ADF1597E656FBE30FEBEFBF0EACE0D9342BD09399FB32B`, confirms one bounded launch, 57 contract items, upload/object/shader/lifecycle PASS, checksum `a915906d`, terminal failure, and clean intentional shutdown.
- Phase I Outcome A proves that the checksum was already the exact successful expected pixel stream; the failure was API error state, not failed sampling or presentation.
- No amendment authorizes a device test, terrain admission, later phase, or additional implementation.

## References

- [Current state](../Documentation/CURRENT_STATE.md)
- [DEC-001](../Decisions/DEC-001.md)
- [DEC-002](../Decisions/DEC-002.md)
- [DEC-003](../Decisions/DEC-003.md)
- [DEC-004](../Decisions/DEC-004.md)
- [WO-056 evidence manifest](../Evidence/WO-056/manifest.md)

## Orchestrator acceptance and Phase J addendum

WO-056 Phase I Outcome A is accepted. The source-proven GL4ES uniform-classification defect, VBO correction, checksum derivation, validation suite, exact-pin replay, successful arm64 workflow, artifact identity, and two-ledger report are accepted as build-qualified diagnostics evidence. This acceptance does not device-accept Bundle 116 and does not admit Diffusion terrain or gameplay.

### Phase J objective

Qualify or reject the already-built Bundle 116 native texture-array sampling/readback contract on one physical iOS device through the preserved normal Diffusion game-information bootstrap. Reuse the existing IPA exactly; do not create a new candidate, workflow, artifact, upload, patch, instrumentation change, or source edit.

### Exact candidate

- Candidate commit: `bc4b2b7181b3111053f14ff86e8ff634718acf30`
- Workflow: `32489923843`, success
- Artifact: `Xash3DiOS-arm64-unsigned`, ID `9449473335`
- IPA: `xash3d-fwgs-ios-arm64.ipa`, `8,716,506` bytes
- IPA SHA-256: `4FD8D67DDAEBF1986AC795164B7CD20BA782319B9F29200C9EA76F1A4BA73806`
- Locked arguments: `-dev 2 -log -game diffusion -ref gl4es -gl4es_texture_array_selftest`

### Authorized device procedure

1. Verify the IPA filename, byte count, and SHA-256 before installation. A mismatch is Outcome C; do not run it.
2. Preserve the device's existing authorized external Diffusion data. Do not repackage, replace, add, or delete game data for this test.
3. Install Bundle 116 over the existing test installation without deleting its data.
4. Launch the diagnostic route exactly once. Do not click Start, enter gameplay, select a map, or interact with menus. The harness is expected to terminate the app intentionally after its bounded result.
5. Export the complete new engine log from that one launch. Record the device model, iOS version, and whether the process visibly closed after the terminal marker.
6. If the process terminates without a complete terminal marker, also return the matching iOS `.ips` report if one exists. Do not infer a hard crash merely from the app closing.
7. Stop after the evidence handoff. Do not repeat the launch unless a later orchestrator instruction explicitly authorizes it.

### Outcome A — device acceptance

Accept Bundle 116 only if the one complete log proves the normal Diffusion bootstrap, all 57 contract items, one renderer dispatch, every retained policy/object/upload/shader/lifecycle PASS stage, `sampling-fbo ... result=PASS`, every immediate `sampling-call` with `error=0x0000 result=PASS`, the exact sampling contract, checksum `a915906d`, terminal `PASS failures=0 diffusion_started=0`, and bounded intentional shutdown. This qualifies the native texture-array conformance harness only. Terrain remains disabled and unadvertised; gameplay and `ch1map1` remain outside the result.

### Outcome B — device rejection

Reject Bundle 116 if a complete run reaches the harness but records any nonzero immediate error, framebuffer failure, checksum other than `a915906d`, missing retained PASS stage, terminal FAIL, nonzero failure count, nonzero `diffusion_started`, module/map/terrain/gameplay admission, or a matching iOS crash report. Preserve the exact first failing call and all later bounded evidence. Do not patch, rebuild, or rerun.

### Outcome C — inconclusive evidence

Stop as inconclusive if the IPA identity is wrong, external Diffusion game information is unavailable, installation or log export fails, the log is truncated before the decisive boundary without a matching `.ips`, or the evidence cannot be tied to one Bundle 116 launch. Do not reinterpret this as renderer failure and do not rerun without review.

### Required evidence

- Verified IPA filename, size, and SHA-256
- Device model and iOS version
- Complete engine log from the sole authorized launch, with its filename, byte count, and SHA-256
- Exact first divergent marker if Outcome B applies
- Matching `.ips` only if unexpected process termination occurred and one exists
- Explicit Outcome A, B, or C conclusion and stop state

### Prohibited changes and stop conditions

No source, patch, validator, CI, workflow, artifact, IPA, game-data, renderer, terrain, menu, input, gameplay, or `ch1map1` change is authorized. Do not test Bundle 114, do not launch ordinary gameplay, and do not begin another phase. The worker may request the one evidence package above, validate it, update the durable records, and report; it must otherwise remain stopped.
